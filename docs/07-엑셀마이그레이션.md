# 07. 엑셀 마이그레이션

> 대상 파일: `이우드림무역 05월 2026.xlsx` (2026-05-04 ~ 09-11, 약 4개월)
> 전 시트·전 셀·**수식까지** 분석 완료. 아래는 확정 매핑표입니다.

---

## 1. 원칙

| # | 원칙 | 이유 |
|---|------|------|
| 1 | 원본 행을 통째로 보존 (`import_rows.raw_json`) | "엑셀 몇 번째 줄이 어느 주문이 되었나" 역추적 |
| 2 | **수식 원문도 보존** (`import_rows.raw_formula`) ⭐ | 환율이 수식에만 있어서 근거 필요 |
| 3 | 1행 = 1주문으로 먼저 생성 후 병합 | 자동 추론으로 잘못 묶는 것보다 안전 |
| 4 | 배치 단위 롤백 | |
| 5 | 애매한 데이터는 버리지 않고 `HOLD` | Sheet3 |
| 6 | 마이그레이션 주문은 `ORDER-MIG-...` 로 구분 | |
| 7 | **마진·마진율은 가져오지 않음** ⭐ | 분모가 지출금액이라 재계산이 맞음. 대조용으로만 사용 |

---

## 2. 가져오기 6단계

```
STEP 1  업로드        .xlsx → import_batches 생성
STEP 2  시트 감지     6개 시트 인식 → 처리 대상 선택
STEP 3  ⭐ 수식 파싱   D열 수식 `=C3/218.22` 에서 환율 218.22 추출
STEP 4  컬럼 매핑     자동 추천 + 수동 수정
STEP 5  검증 미리보기  ✅정상 / ⚠️경고 / ❌오류 / ⏸보류
STEP 6  실행          트랜잭션 일괄 생성 / [배치 되돌리기]
```

### STEP 3 — 환율 수식 파싱 ⭐ (이 파일 전용 필수 단계)

일반통장 D열, 법인통장 D열의 수식에서 환율을 꺼냅니다.

```python
# D3 = "=C3/218.22"  →  fx_rate = 218.22, fx_rate_source = 'PARSED'
m = re.search(r'=\s*[A-Z]+\d+\s*/\s*([\d.]+)', formula)
```

| 시트 | 파싱 대상 | 추출되는 환율 |
|------|----------:|--------------|
| 일반통장 | 64건 | 48종 (201.93 ~ 231.57) |
| 법인통장 | 476건 | 132종 (201.17 ~ 231.57) |

**⚠️ 헤더의 `환율적용 192` / `195`는 무시합니다.** 실제 수식 값과 30 가까이 차이 납니다.

파싱 실패 행은 `HOLD` 처리하고, `입금액 ÷ CNY환산액`으로 역산한 값을 제안합니다.

---

## 3. 시트별 확정 매핑

### 3-1. `해외송금` (446행) → 해외송금 루트

| 엑셀 열 | 헤더 | → 대상 | 규칙 |
|---------|------|--------|------|
| A | 일자 | `orders.order_date`, `receipts.receipt_date` | **누락 230행** → 직전 행 값 forward-fill + `추정` 플래그 |
| B | 거래처 | `orders.partner_id` | 별칭 테이블로 정규화. `이우드림`은 **내부 자금이동으로 분리** ⭐ |
| C | USD (이보이스) | `receipts.usd_amount`, `orders.usd_invoice_amount` | 참고값 |
| D | CNY (도착금액) | `receipts.amount` (CNY) + `splits`(SALES) | **마진 기준** |
| E | CNY (지출금액) | `expenses`(GOODS, CNY) + 배분 | 0/빈칸이면 행 생성 안 함 |
| F | 인건비용 | `expenses`(LABOR_CN, CNY) + 배분 | 48건 |
| G | 기타비용 | `expenses`(ETC_ORDER, CNY) + 배분 | 10건 |
| H | 마진 | — | **가져오지 않음.** 재계산값과 대조만 |
| I | % | — | 〃 (분모가 지출금액) |
| J | (메모) | `orders.memo` | 3건 (`30프로받음 9630 지불` 등) |

**고정값**: `route=OVERSEAS`, `entity=CN`, `settlement_currency=CNY`, `deal_type=OVERSEAS_DIRECT`

**⭐ 이우드림 16행 분리**

```
B열 = "이우드림"  →  internal_transfers 테이블로
  transfer_date = A열, usd_amount = C열, cny_arrival_amount = D열
  fx_rate_usd_cny = D ÷ C  (6.7481 ~ 6.8028)
  from_entity = KR, to_entity = CN
→ 주문·입금·매출 어디에도 생성하지 않음
→ 합계 USD 1,110,000 / CNY 7,516,792
```

### 3-2. `일반통장` (110행) → 일반통장 루트

| 엑셀 열 | 헤더 | → 대상 | 규칙 |
|---------|------|--------|------|
| A | 일자 | `order_date` / `receipt_date` | 누락 21행 → forward-fill |
| B | 거래처 | `partner_id` | 11곳 (에이스 57건) |
| C | KRW 입금액 | `receipts.amount` (KRW) | 66건 |
| D | CNY(환율적용 192) | `receipts.amount_cny` + **`fx_rate`(수식 파싱)** ⭐ | 헤더 192 무시 |
| E | CNY(지출금액) | `expenses`(GOODS, **CNY**) + 배분 | 111건 |
| F | 대행통관비용 | `expenses`(CUSTOMS, **CNY**) + 배분 | 5건 |
| G | 인건비용 | `expenses`(LABOR_CN, CNY) | 데이터 0건 (헤더만) |
| H | 기타비용 | `expenses`(ETC_ORDER, CNY) | 데이터 0건 |
| I / J | 마진 / % | — | 대조용 |
| K | (메모) | `orders.memo` | `收50万 结2107.5` 등 2건 |

**고정값**: `route=BANK_GEN`, `entity=KR`, `settlement_currency=KRW`, `deal_type=GEN_DEPOSIT`

**⭐ 예치금 기초잔액 처리 (필수)**

이 시트는 **지출 합계(CNY 1,712,554)가 입금 환산액(CNY 1,448,423)보다 CNY 264,131 많습니다.**
이전 기간에 받아둔 예치금에서 나간 것이므로, 기초잔액을 넣지 않으면 전체가 적자로 보입니다.

| 방법 | 처리 |
|------|------|
| **권장** | 마이그레이션 시점의 거래처별 예치금 잔액을 담당자가 입력 → `deposit_ledger` `ADJUST` 기초행 생성 |
| 대안 | 입금만 있고 지출이 없는 행을 `DEPOSIT_GENERAL` 적립으로 처리 |

> ⚠️ 대표님께 **에이스·더놀자·아뜰리에독립 등 11개 거래처의 현재 예치금 잔액**을 여쭙습니다.

### 3-3. `법인통장` (501행) → 법인통장 루트

| 엑셀 열 | 헤더 | → 대상 | 규칙 |
|---------|------|--------|------|
| A | 일자 | | 누락 192행 → forward-fill |
| B | 거래처 | `partner_id` + **`orders.external_ref`** ⭐ | `아르미르샵49` → 거래처 `아르미르샵`, external_ref `아르미르샵49` |
| C | KRW 입금액 | `receipts.amount` | 484건 |
| D | CNY(환율적용 195) | `amount_cny` + **`fx_rate`(수식 파싱)** | 헤더 195 무시. 실제 132종 |
| E | CNY(지출금액) | `expenses`(GOODS) + 배분 | 499건 |
| F | 대행통관비용 | `expenses`(CUSTOMS) + 배분 | 50건 |
| G | 인건비용 | `expenses`(LABOR_CN) + 배분 | 42건 |
| H | 기타비용 | `expenses`(ETC_ORDER) + 배분 | 21건 |
| I / J | 마진 / % | — | 대조용 |
| K | (메모) | `orders.memo` | 4건 |
| L | (빈 열) | — | 무시 |
| **M** | (헤더 없음) | **`invoices.total_amount`** ⭐ | 114건. 값이 있으면 세금계산서 생성 |
| **N** | (헤더 없음) | **`invoices.supply_amount`** ⭐ | `=M/1.1` |

**고정값**: `route=BANK_CORP`, `entity=KR`, `settlement_currency=KRW`

**⭐ M·N 열 → 세금계산서 변환 규칙 (확정)**

```
M열에 값이 있으면:
   deal_type       = CORP_FULL
   invoices.total_amount  = M
   invoices.supply_amount = M / 1.1
   invoices.vat_amount    = M − M/1.1
   invoices.vat_mode      = 'EXCLUDED'
   invoices.issue_status  = 'ISSUED'
   orders.invoice_status  = 'ISSUED'

M열이 비어 있으면 (369건):
   deal_type       = CORP_NOBILL
   invoices 생성 안 함
   orders.invoice_status = 'NONE'
   accounting_class = '상품매출'   ← 회계상으로는 여전히 매출
```

**검증 결과 (예상)**

| 항목 | 엑셀 | 시스템 |
|------|-----:|-------:|
| 세금계산서 건수 | 114 | 114 ✓ |
| 합계금액 | 247,456,552 | 247,456,552 ✓ |
| 공급가액 | 224,960,502 | 224,960,502 ✓ |
| 부가세 | (없음) | 22,496,050 (신규 산출) |

### 3-4. `Sheet1` (24행) → 중국 운영비 4갈래

옆으로 나란한 4개 블록을 분리합니다.

| 엑셀 열 | 블록 | → 대상 | 규칙 |
|---------|------|--------|------|
| A | 직원명 | `employees.name_cn` | 18명. 마스터로 승격 |
| B | 기본급 | `payrolls.base_salary` | 합계 111,450 |
| C | 실지급 | `payrolls.actual_paid` | 합계 116,300 ← **집계 기준** ⭐ |
| D | 社保 | `payrolls.insurance_company` | 4명만 (1,416 / 1,416 / 416 / 1,416) |
| F | 临时工费用 기간 | `expenses.work_period_from/to` | **전각문자 변환 필요** ⭐ |
| G | 임시공 금액 | `expenses`(TEMP_LABOR, CNY) | 5건 합계 69,538 |
| J | 사무실경비 날짜/내용 | `expenses.expense_date` 또는 `work_desc` | **날짜 3건 + 텍스트 2건 혼재** ⭐ |
| K | 사무실경비 금액 | `expenses`(OFFICE, CNY) | 5건 합계 33,055 |
| 24행 | 合计 | — | 가져오지 않음 (재계산) |
| 25행 | 9月总合 | — | 대조용 |

**⭐ 전각문자 기간 변환**

```
"１２／２８－１／３"  → NFKC 정규화 → "12/28-1/3"
                    → 연도 추정 → 2025-12-28 ~ 2026-01-03
```

연도가 없으므로 **담당자 확인 후 확정**합니다.

**⭐ 사무실 경비 J열 혼재 처리**

| J열 원본 | 타입 | 처리 |
|----------|------|------|
| 46031 | 날짜 시리얼 | `2026-01-09` |
| 46047 | 날짜 시리얼 | `2026-01-25` |
| 46056 | 날짜 시리얼 | `2026-02-03` |
| `박스비8/9월` | 텍스트 | `work_desc` 로. 날짜는 `⚠️미상` |
| `js 8월항공비` | 텍스트 | 〃 |

**⚠️ 기간 혼재 경고**

Sheet1은 `9月总合`인데 임시공 기간은 12/28~1/31이고 사무실 경비 날짜는 1~2월입니다. 거기에 8~9월 항목이 섞여 있습니다.
→ **급여의 `귀속월`을 담당자가 직접 지정**하도록 미리보기 화면에서 요청합니다.

### 3-5. `Sheet2` → 가져오지 않음

`A1` 한 칸만 있고 값이 없는 **완전히 빈 시트**입니다. 체크박스 기본 해제.

### 3-6. `Sheet3` (13행) → ⏸ 보류 테이블 ⚠️

| 거래처 | 금액 | C열 | 추정 통화 |
|--------|-----:|-----|----------|
| 더라임커머스 | 112,848 | | CNY |
| 커스텀드리다 | 7,337 | | CNY |
| 홀릭 | 5,700 | | CNY |
| 준코 | 20,000 | | CNY |
| 우아샵 | 16,000 | | CNY |
| **아둥놀이터** | **4,391** | **美金** | **USD** ✓ |
| 아르미르샵 | 9,630,000 | | **KRW** (자릿수) |
| **코러스 코리아** | **5,755** | **美金** | **USD** ✓ |
| 래이디박스 | 300,000 | | CNY? |
| 도도하라 | 170,000 | | CNY? |
| 재이든 | 36,000 | | CNY |
| 다온글로벌 | 30,000 | | CNY |
| 에이스 | 40,000 | | CNY |

**분석으로 좁혀진 범위**

- `美金` 표시 2건은 확실히 USD
- 아르미르샵 9,630,000은 자릿수로 보아 KRW. 해외송금 메모 `30프로받음 9630 지불`과 연관 가능성
- 다른 시트 메모 패턴(`定金已付`=계약금 지불, `30프로 받음`, `收50万 结2107.5`)으로 미루어 **거래처별 선금 또는 미수 잔액**으로 추정

**처리**

1. 13행 전부 `import_rows`에 `HOLD` 상태로 적재
2. `엑셀 가져오기 > 보류 데이터` 화면에서 행별로 담당자가 지정:
   - 통화 (CNY / USD / KRW) — `美金` 2건은 USD로 미리 채워둠
   - 성격 → 아래 중 선택

| 성격 | 반영 |
|------|------|
| 거래처별 **미수금** | 진행중 주문의 미정산 잔액으로 반영 |
| 거래처별 **예치금 잔액** | `deposit_ledger` `ADJUST` 기초행 생성 |
| **선금(定金) 지급 잔액** | 선급금 전표로 생성 |
| 누적 거래액 요약 | 참고 메모만, 원장 미반영 |
| 의미 없음 | `SKIP` |

3. 확정 전까지 **어떤 집계에도 반영되지 않습니다.**

---

## 4. 거래처 마스터 생성

### 4-1. 별칭 정규화 (16그룹)

```
정규화 키 = NFKC 정규화 → 공백 제거 → 끝 숫자 제거
```

| 정식 명칭 | 흡수할 별칭 |
|-----------|------------|
| 코러스코리아 | `코러스 코리아`(10) `코러스코리아`(12) |
| 커스텀드리다 | `커스텀드리다`(10) `커스텀 드리다`(11) |
| 트레이드윈드 | `트레이드윈드`(11) `트레이드 윈드`(10) |
| 세모쇼핑백 | `세모쇼핑백`(46) `세모 쇼핑백`(2) |
| 행복한레시피 | `행복한 레시피`(15) `행복한레시피`(11) |
| 조안코퍼레이션 | `조안코퍼레이션`(16) `조안 코퍼레이션`(5) |
| 다온글로벌 | `다온글로벌`(30) `다온 글로벌`(3) |
| 네일의품격 | `네일의품격`(53) `네일의 품격`(1) |
| 더라임커머스 | `더라임 커머스`(6) `더라임커머스`(7) |
| 제이엔에스테크 · 브리즈티앤씨 · 에스에스알컴퍼니 · 씨앤아이물류 외 | 각 2종 |

→ 자동 병합하지 않고 **병합 후보로 제시**, 담당자 확인 후 확정합니다.

### 4-2. 숫자 꼬리표 분리 ⭐

```
아르미르샵49 ~ 아르미르샵133  (82건)
  → partners.name       = "아르미르샵"       (1곳)
  → orders.external_ref = "아르미르샵49" 등  (82건 각각 보존)
```

### 4-3. 내부 계정 표시

```
이우드림  →  partners.is_internal = TRUE
          →  모든 매출·마진·순위 집계에서 제외
```

### 4-4. 기타

- 거래처 코드 자동 채번 (`P0001`~)
- `default_route` = 가장 많이 등장한 시트의 루트
- 원문 237종 → 정규화 후 약 200곳 예상

---

## 5. 검증 리포트 (STEP 5)

| 검증 | 결과 | 이 파일 예상 건수 |
|------|------|-----------------:|
| 필수값(거래처/금액) 누락 | ❌ 오류 | 소수 |
| **일자 누락 → forward-fill** | ⚠️ 경고 + `추정` 플래그 | **443건** |
| **환율 수식 파싱 실패** | ⏸ 보류 | 0 예상 |
| 원본 마진 ≠ 재계산 마진 (±1 초과) | ⚠️ 경고 | 반올림 다수 |
| **USD 금액 이상치** | ⚠️ 경고 | **1건** (해외송금 65행 22,446,983.52) |
| **수식이 다른 행 참조** | ⚠️ 경고 | **1건** (법인통장 319행 `E321` 참조) |
| **M ÷ 입금 ≠ 1.1** | ⚠️ 경고 | **1건** (법인통장 496행, 비율 2.0158) |
| 거래처명 신규 | ⚠️ 경고 | 별칭 병합 후 표시 |
| 거래처명 유사 (병합 후보) | ⚠️ 경고 | **16그룹** |
| 마진 음수 | ⚠️ 경고 | 322건 → 주문 병합 후 재계산 |
| 마진율 #DIV/0! | — | 49건 → `—`로 표시 |
| 내부 계정(`이우드림`) | ℹ️ 분리 | **16건** → internal_transfers |
| Sheet3 통화 미상 | ⏸ 보류 | **11건** (美金 2건은 확정) |
| Sheet1 귀속월 미상 | ⏸ 보류 | 급여 18행 · 임시공 5행 |

---

## 6. 가져오기 후 대조표

```
                     엑셀 원본          시스템            차이
─────────────────────────────────────────────────────────────
해외송금 CNY 도착   22,272,750.00   14,755,958.00   -7,516,792  ⚠️
  └ 이우드림 분리분                   7,516,792  ← 내부 자금이동
해외송금 CNY 지출    9,051,230.51    9,051,230.51            0  ✓
해외송금 인건비         61,528.30       61,528.30            0  ✓
해외송금 기타비         51,394.95       51,394.95            0  ✓

일반통장 KRW 입금  318,254,874.50  318,254,874.50            0  ✓
일반통장 CNY 지출    1,712,554.01    1,712,554.01            0  ✓

법인통장 KRW 입금 1,150,269,187.31 1,150,269,187.31          0  ✓
법인통장 CNY 지출    4,823,625.53    4,823,625.53            0  ✓
법인통장 세금계산서 247,456,552.32  247,456,552.32            0  ✓

Sheet1 급여(실지급)    116,300.00      116,300.00            0  ✓
Sheet1 사회보험          4,664.00        4,664.00            0  ✓
Sheet1 임시공           69,538.00       69,538.00            0  ✓
Sheet1 사무실경비       33,055.00       33,055.00            0  ✓
Sheet1 총합            218,707.00      223,557.00       +4,850  ⚠️
  └ 엑셀은 기본급(111,450), 시스템은 실지급(116,300) 기준
```

> 차이가 나는 두 항목은 **오류가 아니라 의도된 설계 변경**입니다. 대조표에 사유를 함께 표시합니다.

---

## 7. 이관 이후 권장 순서

```
1. 엑셀 가져오기 (과거 4개월)
2. 계좌 기초잔액 입력
3. ⭐ 거래처별 예치금 기초잔액 입력  → 일반통장 11곳 필수
4. ⭐ 거래유형 세무 규칙 확정         → 사이트 부가세 방식만 미정
5. ⭐ Sheet3 보류 데이터 의미 확정    → 13행
6. 거래처 별칭 병합 확정             → 16그룹
7. 대조표로 엑셀 vs 시스템 확인
8. 신규 거래는 시스템에만 입력 (엑셀 병행 1개월 → 이후 중단)
```
